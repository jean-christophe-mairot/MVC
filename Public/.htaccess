# on demarre le moteur de réécriture
RewriteEngine on

# on réécrit l'URL ecriture sous forme de regex
# autorise de a à z et de A à Z de 0 à 9 le tire et le underscor et slash *=pour tous les caractères
# on redirige vers index.php
# $1 pour le premier groupe d'ecriture
RewriteRule ^([a-zA-Z0-9\-\_\/]*)$ index.php?p=$1